{% extends "fullwidth.twig" %}

{% block content %}

    <script src="/node_modules/chart.js/dist/Chart.bundle.min.js"></script>
    <script type = "text/javascript" src = "/node_modules/wordcloud/src/wordcloud2.js"></script>
    <script type = "text/javascript" language = "javascript">

    //Function to get hares for the event
    function getVirginHash(hasher_id){
     var formData = {hasher_id:hasher_id};
     $.ajax({
         url : "/{{kennel_abbreviation}}/statistics/hasher/firstHash",
         type: "POST",
         data : formData,
         dataType : 'json',
         success: function(data, textStatus, jqXHR){

          var virginHashEventNumber = "<a href=/{{kennel_abbreviation}}/hashes/" +data.HASH_KY+ ">" + data.KENNEL_EVENT_NUMBER + "</a>";
          $('#virgin_hash_hash_number').empty();
          $('#virgin_hash_hash_number').append(virginHashEventNumber);
          //$('#virgin_hash_hash_number').listview('refresh');

          var virginHashEventDate = data.EVENT_DATE_FORMATTED;
          $('#virgin_hash_event_date').empty();
          $('#virgin_hash_event_date').append(virginHashEventDate);

          var virgin_hash_location = data.EVENT_LOCATION;
          $('#virgin_hash_location').empty();
          $('#virgin_hash_location').append(virgin_hash_location);


          var years_elapsed = data.YEARS_SINCE_EVENT;
          var months_elapsed = data.MONTHS_SINCE_EVENT;
          var days_elapsed = data.DAYS_SINCE_EVENT;
          var time_elapsed = years_elapsed + " years, " + months_elapsed + " months, " + days_elapsed + " days";
          $('#virgin_hashing_elapsed_time').empty();
          $('#virgin_hashing_elapsed_time').append(time_elapsed);

         },
         error: function (jqXHR, textStatus, errorThrown){
              alert("failure");
              alert(jqXHR);
              alert(textStatus);
              alert(errorThrown);
         }
     });
   }

   //Function to get hares for the event
   function getMostRecentHash(hasher_id){
    var formData = {hasher_id:hasher_id};
    $.ajax({
        url : "/{{kennel_abbreviation}}/statistics/hasher/mostRecentHash",
        type: "POST",
        data : formData,
        dataType : 'json',
        success: function(data, textStatus, jqXHR){

         var latestHashEventNumber = "<a href=/{{kennel_abbreviation}}/hashes/" +data.HASH_KY+ ">" + data.KENNEL_EVENT_NUMBER + "</a>";
         $('#latest_hash_hash_number').empty();
         $('#latest_hash_hash_number').append(latestHashEventNumber);
         //$('#virgin_hash_hash_number').listview('refresh');

         var latestHashEventDate = data.EVENT_DATE_FORMATTED;
         $('#latest_hash_event_date').empty();
         $('#latest_hash_event_date').append(latestHashEventDate);

         var latest_hash_location = data.EVENT_LOCATION;
         $('#latest_hash_location').empty();
         $('#latest_hash_location').append(latest_hash_location);


         var years_elapsed = data.YEARS_SINCE_EVENT;
         var months_elapsed = data.MONTHS_SINCE_EVENT;
         var days_elapsed = data.DAYS_SINCE_EVENT;
         var time_elapsed = years_elapsed + " years, " + months_elapsed + " months, " + days_elapsed + " days";
         $('#latest_hashing_elapsed_time').empty();
         $('#latest_hashing_elapsed_time').append(time_elapsed);

        },
        error: function (jqXHR, textStatus, errorThrown){
             alert("failure");
             alert(jqXHR);
             alert(textStatus);
             alert(errorThrown);
        }
    });
  }







//Generic function to retrieve data
function getListData(hasher_id,url_value,div_value, column_one_header, column_two_header){
 var formData = {hasher_id:hasher_id};
 $.ajax({
     url : url_value,
     type: "POST",
     data : formData,
     dataType : 'json',
     success: function(data, textStatus, jqXHR){
       $(div_value).empty();

       $(div_value).append('<table class="table table-condensed">');
       $(div_value).append('<thead>');
       $(div_value).append('<tr><td><b>' +column_one_header+'</b></td><td>&nbsp;' +column_two_header+'</td></tr>');
       $(div_value).append('</thead>');



       $.each(data, function(key,row) {
         $(div_value).append('<tr>');
         $(div_value).append('<td><b>'+ row.THE_VALUE + '</b></td>');
         $(div_value).append('<td>&nbsp;'+ row.THE_COUNT + '</td>');
         $(div_value).append('</tr>');
       });

       $(div_value).append('</table>');

     },
     error: function (jqXHR, textStatus, errorThrown){
          alert("failure");
          alert(jqXHR);
          alert(textStatus);
          alert(errorThrown);
     }
 });
}


   //Call these functions upon page load
   $(document).ready(function() {
      getVirginHash({{hasherValue.HASHER_KY}});
      getMostRecentHash({{hasherValue.HASHER_KY}});
      //getListData({{hasherValue.HASHER_KY}},"/{{kennel_abbreviation}}/statistics/hasher/hashes/by/city","#hasherHashesByCity-list","City","Count");
      getListData({{hasherValue.HASHER_KY}},"/{{kennel_abbreviation}}/statistics/hasher/all/harings/by/city","#hasherHaringsByCity-list","City","Count");
      getListData({{hasherValue.HASHER_KY}},"/{{kennel_abbreviation}}/coharecount/byhare/allhashes","#cohareCountByHareAllHashes-list","Hasher","Count");
    });

   </script>




    <div class="container">
      <h1>{{pageTitle}}</h1>
      <h5>
        <i><a href='/{{kennel_abbreviation}}/hashers/{{hasherValue.HASHER_KY}}?name={{hasherValue.HASHER_NAME|url_encode}}'>View Hound Details</a></i> |
        Overall Hare Details |
        <i><a href='/{{kennel_abbreviation}}/hares/true/{{hasherValue.HASHER_KY}}?name={{hasherValue.HASHER_NAME|url_encode}}'>True Hare Details</a></i> |
        <i><a href='/{{kennel_abbreviation}}/hares/hyper/{{hasherValue.HASHER_KY}}?name={{hasherValue.HASHER_NAME|url_encode}}'>Hyper Hare Details</a></i>

      </h5>



      <div class="row">

        <div class="col-sm-5" style="background-color:lime;">
          <h3>Basic Details</h3>
          <dl class="dl-horizontal">
              {% autoescape %}
                <dt>Hasher Name</dt><dd> {{hasherValue.HASHER_NAME}}</dd>
                <dt>Hasher Abbreviation</dt><dd> {{hasherValue.HASHER_ABBREVIATION}} </dd>
                <dt>Last Name</dt><dd> {{hasherValue.LAST_NAME|first}}{% for i in 2..hasherValue.LAST_NAME|length%}{{'*'}}{% endfor %} </dd>
                <dt>First Name</dt><dd> {{hasherValue.FIRST_NAME|first}}{% for i in 2..hasherValue.FIRST_NAME|length%}{{'*'}}{% endfor %}</dd>
                {% if hasherValue.DECEASED == "0000000001" %}
                  <dt>Deceased</dt><dd> Sadly, yes.</dd>
                {% endif %}

                <dt>Total Haring Count</dt><dd> {{overallHareCount}}</dd>
                <dt>True Haring Count</dt><dd>{{trueHareCount}}</dd>
                <dt>Hyper Haring Count</dt><dd>{{hyperHareCount}}</dd>
              {% endautoescape %}
          </dl>
        </div>



        <div class="col-sm-7" style="background-color:silver;">
          <h3>Their harings</h3>
          <dl class="dl-horizontal">
            <a href='/{{kennel_abbreviation}}/listhashes/byhare/{{hasherValue.HASHER_KY}}'>Their harings</a><br>
            <a href='/{{kennel_abbreviation}}/coharelist/byhare/allhashes/{{hasherValue.HASHER_KY}}'>Their Cohares</a><br>
            <a href='/{{kennel_abbreviation}}/coharecount/byhare/allhashes/{{hasherValue.HASHER_KY}}'>Their Cohare Count</a><br>
            All of their haring analversaries (Not written yet)<br>
            <a href='/{{kennel_abbreviation}}/getHasherCountsByHare/{{hasherValue.HASHER_KY}}'>Hounds who have hashed their trails</a><br>
          </dl>
        </div>

      </div>



      <div class="row">
        <div class="col-sm-6">
          <canvas id="myChart2" width="25" height="25"></canvas>
          <script>
          var ctx = document.getElementById("myChart2");
          var myChart = new Chart(ctx, {
              type: 'bar',
              data: {
                  labels: [
                    {% for individualValue in harings_by_year_list %}
                      {{individualValue.THE_VALUE}},
                    {% endfor %}
                  ],
                  datasets: [{
                      label: '# of True Harings',
                      data: [
                        {% for individualValue in harings_by_year_list %}
                          {{individualValue.NON_HYPER_COUNT}},
                        {% endfor %}],
                        backgroundColor:'rgba(54, 162, 235, 0.2)',
                        borderColor:'rgba(54, 162, 235, 1)',
                      borderWidth: 1
                  },
                  {
                      label: '# of Total Harings',
                      data: [
                        {% for individualValue in harings_by_year_list %}
                          {{individualValue.TOTAL_HARING_COUNT}},
                        {% endfor %}],
                        backgroundColor:'rgba(255, 99, 132, 0.2)',
                        borderColor:'rgba(255, 99, 132, 1)',

                      borderWidth: 1
                  }
                ]
              },
              options: {
                  scales: {
                      yAxes: [{
                          ticks: {
                              beginAtZero:true
                          }
                      }]
                  },
                  title: {
                    display: true,
                    text: 'Harings Per Year'
                  }
              }
          });
          </script>
        </div>


        <div class="col-sm-6">

          <canvas id="myChart3" width="25" height="25"></canvas>
          <script>
          var ctx = document.getElementById("myChart3");
          var myChart = new Chart(ctx, {
              type: 'bar',
              data: {
                  labels: [
                    {% for individualValue in harings_by_month_list %}
                      "{{individualValue.MONTH_NAME[0:3]}}",
                    {% endfor %}
                  ],
                  datasets: [{
                      label: '# of True Harings',
                      data: [
                        {% for individualValue in harings_by_month_list %}
                          {{individualValue.NON_HYPER_COUNT}},
                        {% endfor %}],
                        backgroundColor:'rgba(127, 15, 239, 0.2)',
                        borderColor:'rgba(127, 15, 239, 1)',
                      borderWidth: 1
                  },
                  {
                      label: '# of Total Harings',
                      data: [
                        {% for individualValue in harings_by_month_list %}
                          {{individualValue.TOTAL_HARING_COUNT}},
                        {% endfor %}],
                        backgroundColor:'rgba(75, 192, 192, 0.2)',
                        borderColor:'rgba(75, 192, 192, 1)',

                      borderWidth: 1
                  }
                ]
              },
              options: {
                  scales: {
                      yAxes: [{
                          ticks: {
                              beginAtZero:true
                          }
                      }]
                  },
                  title: {
                    display: true,
                    text: 'Harings Per Month'
                  }
              }
          });
          </script>
        </div>
      </div>

      <div class="row">
        <div class="col-sm-6">

          <canvas id="myChart4" width="25" height="25"></canvas>
          <script>
          var ctx = document.getElementById("myChart4");
          var myChart = new Chart(ctx, {
              type: 'bar',
              data: {
                  labels: [
                    {% for individualValue in harings_by_quarter_list %}
                      "{{individualValue.THE_VALUE}}",
                    {% endfor %}
                  ],
                  datasets: [{
                      label: '# of True Harings',
                      data: [
                        {% for individualValue in harings_by_quarter_list %}
                          {{individualValue.NON_HYPER_COUNT}},
                        {% endfor %}],
                        backgroundColor:'rgba(127, 15, 239, 0.2)',
                        borderColor:'rgba(127, 15, 239, 1)',
                      borderWidth: 1
                  },
                  {
                      label: '# of Total Harings',
                      data: [
                        {% for individualValue in harings_by_quarter_list %}
                          {{individualValue.TOTAL_HARING_COUNT}},
                        {% endfor %}],
                        backgroundColor:'rgba(75, 192, 192, 0.2)',
                        borderColor:'rgba(75, 192, 192, 1)',

                      borderWidth: 1
                  }
                ]
              },
              options: {
                  scales: {
                      yAxes: [{
                          ticks: {
                              beginAtZero:true
                          }
                      }]
                  },
                  title: {
                    display: true,
                    text: 'Harings Per Quarter'
                  }
              }
          });
          </script>
        </div>

        <div class="col-sm-6">

          <canvas id="myChart5" width="25" height="25"></canvas>
          <script>
          var ctx = document.getElementById("myChart5");
          var myChart = new Chart(ctx, {
              type: 'bar',
              data: {
                  labels: [
                    {% for individualValue in harings_by_state_list %}
                      "{{individualValue.EVENT_STATE}}",
                    {% endfor %}
                  ],
                  datasets: [{
                      label: '# of True Harings',
                      data: [
                        {% for individualValue in harings_by_state_list %}
                          {{individualValue.NON_HYPER_COUNT}},
                        {% endfor %}],
                        backgroundColor:'rgba(127, 15, 239, 0.2)',
                        borderColor:'rgba(127, 15, 239, 1)',
                      borderWidth: 1
                  },
                  {
                      label: '# of Total Harings',
                      data: [
                        {% for individualValue in harings_by_state_list %}
                          {{individualValue.TOTAL_HARING_COUNT}},
                        {% endfor %}],
                        backgroundColor:'rgba(132, 234, 183,.2)',
                        borderColor:'rgba(132, 234, 183,1)',

                      borderWidth: 1
                  }
                ]
              },
              options: {
                  scales: {
                      yAxes: [{
                          ticks: {
                              beginAtZero:true
                          }
                      }]
                  },
                  title: {
                    display: true,
                    text: 'Harings Per State'
                  }
              }
          });
          </script>
        </div>
      </div>


      <div class="row">
        <div class="col-sm-6">

          <canvas id="myChart6" width="25" height="25"></canvas>
          <script>
          var ctx = document.getElementById("myChart6");
          var myChart = new Chart(ctx, {
              type: 'bar',
              data: {
                  labels: [
                    {% for individualValue in harings_by_dayname_list %}
                      "{{individualValue.THE_VALUE}}",
                    {% endfor %}
                  ],
                  datasets: [{
                      label: '# of True Harings',
                      data: [
                        {% for individualValue in harings_by_dayname_list %}
                          {{individualValue.NON_HYPER_COUNT}},
                        {% endfor %}],
                        backgroundColor:'rgba(127, 15, 239, 0.2)',
                        borderColor:'rgba(127, 15, 239, 1)',
                      borderWidth: 1
                  },
                  {
                      label: '# of Total Harings',
                      data: [
                        {% for individualValue in harings_by_dayname_list %}
                          {{individualValue.TOTAL_HARING_COUNT}},
                        {% endfor %}],
                        backgroundColor:'rgba(132, 234, 183,.2)',
                        borderColor:'rgba(132, 234, 183,1)',

                      borderWidth: 1
                  }
                ]
              },
              options: {
                  scales: {
                      yAxes: [{
                          ticks: {
                              beginAtZero:true
                          }
                      }]
                  },
                  title: {
                    display: true,
                    text: 'Harings Per Day of the Week'
                  }
              }
          });
          </script>
        </div>

        <div class="col-sm-6">
        </div>
      </div>

      <div class="row">
        <a href="#citylist" class="btn btn-info" data-toggle="collapse">Show Harings By City List</a>
        <a href="#coharelist" class="btn btn-info" data-toggle="collapse">Show Favorite CoHare List</a>
      </div>

      <div class="row">

        <div class="col-sm-6 collapse" style="background-color:yellow" id="citylist">
          <dl class="dl-horizontal">
            <h4>Overall Harings by city</h4>
            <div data-role="content" id="hasherHaringsByCity-list"></div>
          </dl>
        </div>

        <div class="col-sm-6 collapse" style="background-color:gold" id="coharelist">
          <dl class="dl-horizontal">
            <h4>Favorite Overall CoHares</h4>
            <div data-role="content" id="cohareCountByHareAllHashes-list"></div>
          </dl>
        </div>

      </div>


      <div class="row">
        <div id = "div_one" class="col-sm-6">
          <h4>Overall Harings By City</h4>
          <canvas id="my_canvas" class="wordcloud"></canvas>
        </div>
        <div id = "div_two" class="col-sm-6">
          <h4>Favorite Overall CoHares</h4>
          <canvas id="my_canvas2" class="wordcloud"></canvas>
        </div>
      </div>

      <script type = "text/javascript" language = "javascript">
          var canvas = document.getElementById("my_canvas");
          var parent = document.getElementById("div_one");
          canvas.width = parent.offsetWidth;
          canvas.height = parent.offsetHeight;

          var canvas2 = document.getElementById("my_canvas2");
          var parent2 = document.getElementById("div_two");
          canvas2.width = parent.offsetWidth;
          canvas2.height = parent.offsetHeight;
      </script>

      <script>



            var options = {
              list: [
                {% for item in city_haring_count_list %}
                    [
                    "{% autoescape 'js' %}{{item.THE_VALUE}}{% endautoescape %}"
                    ,
                    "{{item.THE_COUNT}}"],
                {% endfor %}
              ],
              drawMask: false,
              shape: "diamond",
              gridSize: 1,
              weightFactor: {{85/city_harings_max_value}}
            };

            WordCloud(document.getElementById('my_canvas'), options );
      </script>

      <script>
            var options2 = {
              list: [
                {% for item in cohare_count_list %}
                    [
                      "{% autoescape false %}{{item.THE_VALUE[:18]}}{% endautoescape %}"
                    ,"{{item.THE_COUNT}}"],
                {% endfor %}
              ],
              drawMask: false,
              shape: "diamond",
              gridSize: 1,
              weightFactor: {{85/cohare_count_max}},
              drawOutOfBound: true
            };

            WordCloud(document.getElementById('my_canvas2'), options2 );
      </script>


      <div class="row"><br><br><br><br>
      </div>
            <div class="row">
              <div id="map" class="col-sm-12"></div>
                <style>
                #map{
                  height: 100%;
                }
                </style>
              </div>
            </div>

            <script>
              var map;
              function initMap(){
                map = new google.maps.Map(document.getElementById('map'),{
                  zoom: 10,
                  center: new google.maps.LatLng( {{avg_lat}}, {{avg_lng}} )
                  //mapTypeId: 'terrain'
                });

                //var latLng = new google.maps.LatLng(2.8, -187.3);
                //var marker = new google.maps.Marker({
              //    position: latLng,
                //  map: map
                //});

              {% for hashEventValue in the_hashes %}
                var marker = new google.maps.Marker({
                  //position: new google.maps.LatLng(7,-188);,
                  position: {lat: {{hashEventValue.LAT}}, lng:{{hashEventValue.LNG}}},
                  map: map
                });
              {% endfor %}

              }
            </script>
            <script async defer src="https://maps.googleapis.com/maps/api/js?key={{geocode_api_value}}&callback=initMap">
            </script>


      <div class="row"><br><br><br><br>
      </div>




    </div>

{% endblock %}
